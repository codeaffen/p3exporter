exporter_name: "Python prammable Prometheus exporter"
collectors:
  - p3exporter.collector.example
  - loadavg
  - netdev
collector_opts:
  netdev:
    whitelist:
    blacklist:
      - docker0
      - lo
logging:
  - name: root
    level: INFO
